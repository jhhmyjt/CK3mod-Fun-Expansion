namespace = hc
hc.001 = {
	desc = hc.001.desc
	left_portrait = {
		character = ROOT
		animation = stress
	}
	theme = crown
	override_background = {
		reference = garden
	}
	trigger = {
		NOT = {
			has_character_modifier = hc_modifier_01
		}
		NOT = {
			has_character_modifier = hc_modifier_02
		}
		NOT = {
			has_trait = god_descent
		}
	}
	option = {
		name = hc.001.a
		add_character_modifier = {
			modifier = hc_modifier_01
			days = 30
		}
		add_character_modifier = {
			modifier = hc_modifier_02
			years = 1
		}
		add_gold = 1000
		trigger_event = {
			id = hc.002
			days = 30
		}
	}
}

hc.002 = {
	desc = hc.002.desc
	left_portrait = {
		character = ROOT
		animation = war_over_win
	}
	theme = crown
	override_background = {
		reference = garden
	}
	trigger = {
		has_character_modifier = hc_modifier_02
		NOT = {
			has_character_modifier = hc_modifier_01
		}
		NOT = {
			has_trait = god_descent
		}
	}
	option = {
		name = hc.002.a
		remove_character_modifier = hc_modifier_02
		add_trait = god_descent
	}
}
